spring:
  datasource:
    url: jdbc:postgresql://localhost:15432/plbk?currentSchema=plbk
    username: plbk
    password: plbk
    driver-class-name: org.postgresql.Driver
  liquibase:
    change-log: classpath:db/changelog/db.changelog-dev.xml
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: true
    show-sql: true
  security:
    oauth2:
      client:
        registration:
          keycloak:
            client-secret: ${KEYCLOAK_CLIENT_SECRET:LbLkIIODlKOPM1inpn9um9rYQGk9NLMAzoCRiEHB8wdRhmaRFWhRV4JC4vozz2JbdFzgAfVl5T4dgYD2ifU9WV}
        provider:
          keycloak:
            issuer-uri: ${KEYCLOAK_ISSUER_URI:http://localhost:8081/realms/pool-booking}

management:
  server.port: ${MANAGEMENT_SERVER_PORT:8079}

logging:
  level:
    root: INFO
    pro.itfray: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
